// Task 7
let num = parseFloat(prompt("Введите число:"));
if (num >= 5 && num <= 10) {
    console.log("Число попадает в диапазон от 5 до 10 включительно");
} else {
    console.log("Число не попадает в диапазон");
}